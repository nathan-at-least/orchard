//! Helpers for building Pollard transactions.

use super::Bundle;

// Abstracts the process of building Pollard components within a transaction.
pub struct BundleBuilder {}

impl BundleBuilder {
    pub fn build() -> Result<Bundle, ()> {
    }
}
